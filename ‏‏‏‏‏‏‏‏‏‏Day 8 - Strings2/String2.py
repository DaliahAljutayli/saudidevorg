#String2

#Removes whitespace from the beginning or the end.
a = " Using strips(),Removes whitespace of beginning/end "
print( a.strip() )

#The length of a string
b = "Using len() ,The length of this string is ⟵ "
print( b , len(b) )

#The string in lower case
c = "Using lower() ,The String in Lower Case "
print(c.lower() )

#The string in Upper case
d = "Using upper() ,The String in Upper Case "
print(d.upper() )

#Replaces a string
e = "Using replace() ,Replaces a string"
print(e.replace("Using replace() ,Replaces a string","⭐") )

#Splits the string into substrings 
f = "Splits the string into substrings using split()"
print(f.split(",") )
